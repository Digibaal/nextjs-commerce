import { Journal } from 'components/maniflow/journal';

export const metadata = {
  title: 'Journaal',
  description:
    'Je routekaart, je Evidence Log en je weekmeter — lokaal opgeslagen in je eigen browser.'
};

export default function JournaalPage() {
  return <Journal />;
}
